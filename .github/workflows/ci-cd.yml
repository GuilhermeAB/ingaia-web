name: CI

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

  workflow_dispatch:

jobs:
  lint:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2

      - name: Install project dependencies
        run: npm install

      - name: Lint check
        run: npm run lint

  deploy:
    runs-on: ubuntu-latest
    needs: [lint]

    steps:
      - name: Deploy
        run: npm run deploy
